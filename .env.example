# Optional: GitHub personal access token
# Without it you get 60 API requests/hour per IP.
# With it you get 5000/hour. Create one at https://github.com/settings/tokens
GITHUB_TOKEN=

# Port for the HTTP server (default: 3000)
PORT=3000
